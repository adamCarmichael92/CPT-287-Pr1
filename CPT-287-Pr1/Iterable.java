
//iterable interface
//copied from lecture_2 notes

public interface Iterable<T> {
    /** Generates (and returns) an iterator positioned at the beginning.
    @return: an iterator positioned at the beginning
	*/
	Iterator<T> iterator();


}
